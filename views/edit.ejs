<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Görev Düzenle</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h2 class="text-center">Görev Düzenle</h2>

        <form action="/todos/<%= todo.id %>/edit" method="POST">
            <div class="mb-3">
                <label for="task" class="form-label">Görev Adı</label>
                <input type="text" name="task" class="form-control" value="<%= todo.task %>" required>
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Açıklama</label>
                <input type="text" name="description" class="form-control" value="<%= todo.description %>" required>
            </div>
            <div class="mb-3">
                <label for="deadline" class="form-label">Son Tarih (Deadline)</label>
                <input type="date" name="deadline" class="form-control" value="<%= new Date(todo.deadline).toISOString().split('T')[0] %>" required> <!-- Yeni eklendi -->
            </div>
            <div class="mb-3">
                <label for="completed" class="form-label">Tamamlandı</label>
                <input type="checkbox" name="completed" <%= todo.completed ? 'checked' : '' %> >
            </div>
            <button type="submit" class="btn btn-primary">Güncelle</button>
        </form>
    </div>
</body>
</html>
